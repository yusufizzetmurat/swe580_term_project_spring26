[
  {
    "type": "function",
    "function": {
      "name": "search_by_content",
      "description": "Search notes by text content.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Text query to search for."
          }
        },
        "required": ["query"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "search_by_tags",
      "description": "Search notes by tags.",
      "parameters": {
        "type": "object",
        "properties": {
          "tags": {
            "type": "array",
            "items": {"type": "string"},
            "description": "List of tags to filter by."
          }
        },
        "required": ["tags"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "search_by_date",
      "description": "Search notes by creation date range.",
      "parameters": {
        "type": "object",
        "properties": {
          "date_from": {
            "type": "string",
            "description": "Start date in YYYY-MM-DD format."
          },
          "date_to": {
            "type": "string",
            "description": "End date in YYYY-MM-DD format."
          }
        },
        "required": ["date_from", "date_to"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_note_by_path",
      "description": "Retrieve a note by its file path.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "File path of the note."
          }
        },
        "required": ["path"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_note_by_title",
      "description": "Retrieve a note by its title.",
      "parameters": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "description": "Title of the note."
          }
        },
        "required": ["title"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_outgoing_links",
      "description": "Get notes that this note links to.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "File path of the source note."
          }
        },
        "required": ["path"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_incoming_links",
      "description": "Get notes that link to this note.",
      "parameters": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "description": "Title of the target note."
          }
        },
        "required": ["title"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_vault_stats",
      "description": "Get vault statistics.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_recent_notes",
      "description": "Get recently modified notes.",
      "parameters": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "description": "Number of notes to return."
          }
        },
        "required": []
      }
    }
  }
]
